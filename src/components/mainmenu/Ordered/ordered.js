import React, { Component } from 'react';
import { View, Image, StyleSheet, TouchableOpacity, Text } from 'react-native';

class Ordered extends Component{
    render(){
        return(
           <View style={{ backgroundColor: '#fcfcfc', flex: 1 }}></View> 
        );
    }
}
export default Ordered;